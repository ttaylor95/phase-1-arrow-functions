
// declare a function called divide; divides 2000 by 100

const divide = (parameter1, parameter2) => parameter1 / parameter2;

divide(2000,100);

// declare an arrow function called square

const square = para1 => para1 * para1;

square(4);

//declare an arrow function called add 

const add = (num1, num2) => num1 + num2;

add(4,5);